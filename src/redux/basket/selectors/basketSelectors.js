export const getBasketItems = (state) => state.basket.items;
export const getBasketError = (state) => state.basket.error;
export const getBasketIsLoading = (state) => state.basket.isLoading;
export const getBasketPrice = (state) => state.basket.totalPrice;
